---
description: MEP session protocol. Always apply. Canonical rules are in MEP.md.
alwaysApply: true
---

# MEP

Read `MEP.md` and obey it. Then read `AGENTS.md` for project identity.

You are **Cursor**, a first-party peer writer of `handoff.md`. Claude Code, GitHub Copilot, OpenAI Codex, and ChatGPT may also write that file. Do not ask the human to relay their sessions.

Hello: tag in (`Tag-out: [active]`) before you respond. Push the tag-in if you can.

EOL: fill your active entry in place. Never force-push `main`. The word `done` is not EOL.

Platform string: `Cursor (IDE)`, `Cursor (Cloud)`, or `Cursor (CLI)`. Owner tag: `[Cursor]`.
