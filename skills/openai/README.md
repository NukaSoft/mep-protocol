# OpenAI Codex / ChatGPT — MEP notes

**Codex** (CLI, IDE, ChatGPT Codex) loads `AGENTS.md` automatically. Copy `templates/AGENTS.md` and `templates/MEP.md`. Optional skill: `templates/agents-skills/mep-relay/SKILL.md` → `.agents/skills/mep-relay/SKILL.md`.

Platform: `Codex (OpenAI)`. Owner tag: `[Codex]`.

**ChatGPT without git** cannot Hello/EOL on the repo. Use `templates/openai/SEED_PROMPT.md`. The model outputs a v2 handoff entry; the operator pastes it onto `handoff.md`. That paste is the remaining meat-puppet step for disconnected OpenAI sessions.

Platform: `ChatGPT (OpenAI)`. Owner tag: `[ChatGPT]`.
